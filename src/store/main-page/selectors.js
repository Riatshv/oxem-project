export const selectAutoCostValue = (state) => state.autoCost;

export const selectPercentValue = (state) => state.percent;

export const selectLeaseTerm = (state) => state.leaseTerm;

export const selectSubmitStatus = (state) => state.submitStatus;